// Generated by CoffeeScript 1.9.3
(function() {
  var app, kodiRemote;

  app = angular.module("kodiRemote", ["ngAria", "ngAnimate", "ngMaterial", "ngMdIcons", "ngRoute", "infinite-scroll", "kodiRemote.controllers", "kodiRemote.services", "kodiRemote.directives", "kodiRemote.tvshows.controllers", "kodiRemote.tvshows.services", "kodiRemote.movies.controllers", "kodiRemote.movies.services"]);

  app.config(function($mdThemingProvider) {
    return $mdThemingProvider.theme("default").primaryPalette("blue").accentPalette("blue");
  });

  app.constant("SERVER", "192.168.0.111");

  app.constant("PORT", 8080);

  app.config(function($routeProvider, $locationProvider) {
    $routeProvider.when("/tvshows", {
      templateUrl: "app/views/tvshows/index.html",
      controller: "TvShowsController"
    }).when("/tvshows/:id/seasons", {
      templateUrl: "app/views/tvshows/seasons/index.html",
      controller: "TvShowSeasonsController"
    }).when("/tvshows/:tvshowid/seasons/:id/episodes", {
      templateUrl: "app/views/tvshows/seasons/episodes/index.html",
      controller: "TvShowSeasonEpisodesController"
    }).when("/movies", {
      templateUrl: "app/views/movies/index.html",
      controller: "MoviesController"
    }).otherwise("/tvshows", {
      templateUrl: "app/views/tvshows/index.html",
      controller: "TvShowsController"
    });
    return $locationProvider.html5Mode(true);
  });

  app.filter("secondsToDateTime", [
    function() {
      return function(seconds) {
        return new Date(1970, 0, 1).setSeconds(seconds);
      };
    }
  ]);

  kodiRemote = window.kodiRemote || (window.kodiRemote = {});

  kodiRemote.Loader = (function() {
    function _Class(scope, service) {
      this.scope = scope;
      this.service = service;
      return;
    }

    _Class.prototype.handleData = function(data) {};

    _Class.prototype.afterCallback = function(data) {};

    _Class.prototype._baseMethod = function(method, params) {
      if (params == null) {
        params = {};
      }
      this.scope.loading = true;
      return this.service[method](params).then((function(_this) {
        return function(data) {
          _this.scope.loading = false;
          _this.handleData(data);
          return _this.afterCallback(data);
        };
      })(this));
    };

    _Class.prototype.index = function(params) {
      if (params == null) {
        params = {};
      }
      return this._baseMethod("index", params);
    };

    _Class.prototype.show = function(params) {
      if (params == null) {
        params = {};
      }
      return this._baseMethod("show", params);
    };

    return _Class;

  })();

}).call(this);
